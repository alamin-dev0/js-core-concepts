console.log(2 === 2);
console.log(2 === 3);
console.log(2 === '2');
// compares the data type first if data type are different it will return false if data types are same then it will compare the value and return true if value is same otherwise false